# Database

Estructura reservada para conexion, migraciones, seeds y repositorios de persistencia.

La base para desarrollo sera MySQL. Para produccion se usara PostgreSQL.

Las migraciones deben mantenerse compatibles con ambos motores o separarse por adaptador cuando una diferencia sea inevitable.
