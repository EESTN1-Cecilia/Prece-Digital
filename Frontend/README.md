# Frontend

El frontend se divide en dos aplicaciones JavaScript:

- `web`: aplicacion React para administracion y PWA.
- `mobile`: aplicacion React Native para asistencia movil, notificaciones y flujos offline acotados.

Ambas deben reutilizar contratos y constantes desde `Shared` cuando corresponda.
